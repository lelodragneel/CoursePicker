package com.example.project.coursepicker;

import com.google.firebase.database.Exclude;
import com.google.firebase.database.IgnoreExtraProperties;

import java.io.Serializable;
import java.util.HashMap;
import java.util.Map;

/**
 * Class that defines how the data will be stored in the
 * Firebase databse. This is converted to a JSON format
 */

public class Lecture implements Serializable {
/*
    public  String number;
    public  String name;
    public  String classDays;
    public  String primaryBuisness; //Fisher, Distributor, Processor, Fish Monger
    public  String address;
    public  String province;

    public Lecture() {
        // Default constructor required for calls to DataSnapshot.getValue
    }

    public Lecture(String number, String name, String classDays, String classTime, String primaryBuisness, String address, String province){
        this.number = number;
        this.name = name;
        this. = ;
        this.primaryBuisness = primaryBuisness;
        this.address = address;
        this.province = province;
    }

    @Exclude
    public Map<String, Object> toMap(){
        HashMap<String, Object> result = new HashMap<>();

        result.put("number", number);
        result.put("name", name);
        result.put("primaryBuisness", primaryBuisness);
        result.put("address", address);
        result.put("province", province);

        return result;
    }
    */
}
